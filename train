def missing(tab):
    somT = ((len(tab)+1)*(len(tab)+2))/2
    somR = sum(tab)
    miss = somT - somR
    if miss != 0:
        return miss
    else:
        return None
    


tab = [1,2,3,5,6]

print(missing(tab))